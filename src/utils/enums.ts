export enum ActivityType {
    Transfer = 'Transfer',
    Staked = 'Staked',
    Unstaked = 'Unstaked',
}

export enum NetworkType {
    ETH = 'Ethereum',
    SOL = 'Solana',
    ADA = 'Cardano',
}

export enum ServiceType {
    Cron = 'Cron',
    PastEvent = 'PastEvent',
    Contract = 'Contract',
}